<div class="show-div">
  <p><span class="show-content">Name: </span><%= @user.name %></p>
  <p><span class="show-content">User Name: </span><%= @user.username %></p>
  <p><span class="show-content">Email: </span><%= @user.email %></p>
  <% if @user.movies.present? %>
    <p>Reviewed Movies✍🏻</p>
    <ul>
      <% @user.movies.each do |movie|%>
        <li><%= movie.title%></li>
      <% end %>
    </ul>
  <% end %>
  <% if @user.liked_movies.present? %>
    <p>Liked Movies💟</p>
    <ul>
      <% @user.liked_movies.each do |movie|%>
        <li><%= movie.title%></li>
      <% end %>
    </ul>
  <% end %>
  <% if current_user? %>
    <%= link_to "Edit", edit_user_path(@user), class: "button" %>
    <%= link_to "Delete", user_path(@user), class: "button", method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
  <%= link_to 'Back to Users List', users_path, class: "button" %>
</div>